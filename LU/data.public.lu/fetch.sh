#!/usr/bin/env sh
# fetch Prix maximaux carburants (gouvernement, réglementé) (LU) live prices -> this dir
DIR="$(CDPATH= cd "$(dirname "$0")" && pwd)"
# regulated max prices published by the government (not per-station)
